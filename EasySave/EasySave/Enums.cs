namespace EasySave
{
    // Enum pour les types de sauvegarde (à déplacer si nécessaire)
    public enum BackupType { Full, Differential }

    // Enum pour les états des tâches (à déplacer si nécessaire)
    public enum JobState { Ready, Running, Completed, Paused, Error }
}